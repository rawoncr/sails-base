module.exports = () => {
  sails.log.info('Creating users')
  // return User.create(_.values(users))
  //   .then(() => sails.log.info('Users created'))
  //   .catch(err => sails.log.error('Error creating users', err))
}
